devs.forEach((item,index) => {
    if(item.location == "Gazipur"){
        console.log(`
        Name : ${item.name}
        Age : ${item.age}
        Skill: ${item.skill}
        
        `);
    }
});